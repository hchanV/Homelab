# pxve-main server todo list

## Next session goal
Verify the Proxmox host hardware, network, and storage layout before proceding 

---

## 1. Confirm Proxmox boots correctly

- Restart pxve-main.
- Confirm it boots into Proxmox automatically.
- Confirm it does not boot into Windows unless manually selected.
- If needed, update BIOS/UEFI boot order.

Proxmox should boot reliably without needing the boot menu every time. If the server restarts after a power outage or update, it should come back on its own.
Probably cus the driver order is wrong need to figure out what drive proxmox was installed on and what drive has windows on it
Also plug in 2 TB drive next time

---

## 2. Confirm network settings

- Verify current Proxmox IP address.
- Verify gateway.
- Verify DNS server.
- Decide whether to keep DNS as `75.75.75.75` or change it to router DNS later.
- Reserve `10.0.0.64` in the router DHCP settings if possible.
- Check if need static ip?

## 3. Confirm pxve-main hardware config

Use the Proxmox web UI:

- pxve-main -> Summary
  - find CPU and RAM here

- pxve-main -> Disks
  - connected drives
  - drive sizes
  - drive models
  - which drive Proxmox is installed on

- Datacenter -> Storage
  - available storage space
  - local storage
  - local-lvm storage

Use pxve-main -> Shell for GPU:

```bash
lspci | grep -Ei "vga|3d|display"

Useful commands:

```bash
ip addr
ip route
cat /etc/network/interfaces

## 4. Collect baseline Proxmox configuration

### System identity
- Hostname:
- Proxmox version:
- Kernel version:
- Install date:
- Management URL:

Why:
This helps identify exactly which system and software version the notes refer to.

Useful commands:

```bash
hostnamectl
pveversion -v

Network configuration
Management IP:
Subnet/CIDR:
Gateway:
DNS server:
Management interface:
Bridge name:
MAC address:
Router DHCP reservation created? yes/no

Boot configuration
Does server boot into Proxmox automatically? yes/no
BIOS/UEFI boot order:
Proxmox boot drive:
Windows drive still present? yes/no/unknown
Secure Boot enabled? yes/no/unknown
UEFI or Legacy boot?


Storage baseline
Physical disks:
Disk models:
Disk sizes:
Disk serial numbers:
Proxmox boot disk:
Existing partitions:
Current Proxmox storage:
local:
local-lvm:
Any disks that should not be wiped:

Hardware inventory
Server make/model:
CPU:
CPU cores/threads:
RAM:
GPU:
Network adapters:
Motherboard, if known:

Repository/update status
Enterprise repo enabled? yes/no
No-subscription repo enabled? yes/no
Last update check:
Any update errors?

Backup readiness
Backup server exists? yes/no
Planned backup target:
Important VMs exist yet? yes/no
Restore test completed? yes/no

Security baseline
Root password stored safely? yes/no
Additional admin user created? yes/no
2FA enabled? yes/no
SSH access enabled? yes/no
Firewall enabled? yes/no

5. Start storing config files etc. on github